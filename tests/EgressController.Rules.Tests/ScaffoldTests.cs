namespace EgressController.Rules.Tests;

/// <summary>
/// Placeholder so this project contributes tests immediately (MTP treats a zero-test run as
/// exit code 8). SRS catalog and sing-box compiler tests are kept in the product boundary.
/// </summary>
public class ScaffoldTests
{
    [Fact]
    public void Skeleton_is_ready() => Assert.True(true);
}
